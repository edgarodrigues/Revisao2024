// See https://aka.ms/new-console-template for more information
Console.WriteLine("Determine o tamanho, a inclusão de símbolos e outras propriedades para a senha a ser criada: ");
if (double.TryParse(Console.ReadLine(), out double tamanho))
{

    Console.WriteLine("Determine a quantidade de caracteres a ser trabalhada: ");
    if (double.TryParse(Console.ReadLine(), out double caracteres))
    {

        if ("O usuário quer senha de apenas números: );
        {
            Console.WriteLine("Senha apenas com número: ");
        }

        if ("O usuário quer apenas letras e caracteres especiais: );
        {
            Console.WriteLine("Senha apenas com letras e caracteres especiais: ");
        }

        if ("O usuário quer números, letras e caracteres especiais: );
        {
            Console.WriteLine("Senha com números, letras e caracteres especiais: ");
        }

        Console.WriteLine("Salve em bkp.TXT: ");
        if (double.TryParse(Console.ReadLine(), out double bkp.TXT))
        {

            Console.WriteLine("Recuperar senhas geradas: ");
            if (double.TryParse(Console.ReadLine(), out double senhas geradas))
            {









